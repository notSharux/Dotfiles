#Terminal
super + Return
        alacritty

#dmenu
super + d
        dmenu_run

#kill
super + f
        bspc node -t {tiled,floating,fullscreen}

#kill
super + c
	bspc node -c

#quit/restart bspwm
 super + shift + {q,r}
 	bspc {quit,wm -r}
 	
	
